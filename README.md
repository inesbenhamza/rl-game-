# RL Game

This project implements a simple Q-learning agent using Python and Gymnasium.

The agent is trained to solve the `Taxi-v3` environment by learning optimal actions through trial and error. The final agent is evaluated by running multiple episodes with rendering.

## dependencies needed to run
 Install dependencies:
   ```bash
   pip install gymnasium numpy pygame